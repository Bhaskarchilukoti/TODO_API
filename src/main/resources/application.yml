server:
  port: 8080 # set the port for your spring boot application

spring:
  profiles:
    active:
  datasource:
    url: jdbc:postgresql://ep-aged-water-a7331mcy-pooler.ap-southeast-2.aws.neon.tech/expensedb?sslmode=require&channel_binding=require'
    #    url: jdbc:h2:mem:expensedb;DB_CLOSE_DELAY=-1  # URL for H2 database in memory mode. DB_CLOSE_DELAY=-1 this ensures database doesn't shut down immediately after the connection loss
    driver-class-name: org.postgresql.Driver # the JDBC Driver for H2
    username: neondb_owner
    password: npg_W3dwbhVL1fXK
  #    platform: h2
  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: update #Automatically creates or
      # update the database schema based on entity classes
    show-sql: true # Enables logging of SQL queries generated by JPA
    database-platform: org.hibernate.dialect.PostgreSQLDialect

